# Core ML/AI dependencies
torch>=2.0.0
openai>=1.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.24.0
tiktoken>=0.5.0
tokenizers>=0.15.0  # HuggingFace tokenizers for custom BPE training
transformers>=4.35.0  # HuggingFace transformers for pre-trained models (GPT-2, etc.)

# Visualization and plotting
matplotlib>=3.7.0

# Web framework (FastAPI + HTMX stack)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.6
tomli>=2.0.0;python_version<"3.11"  # Backport of tomllib for Python < 3.11
slowapi>=0.1.9  # Rate limiting middleware
boto3>=1.34.0  # AWS DynamoDB for conversation persistence
cryptography>=42.0.0  # Fernet encryption for secure API key session cookies

# Development and code quality tools
ruff>=0.1.0
pytest>=7.4.0
pre-commit>=3.4.0
httpx>=0.26.0  # For testing FastAPI

# Optional: Additional useful packages for ML development
# Uncomment as needed:
# jupyter>=1.0.0
# ipykernel>=6.25.0
# tensorboard>=2.13.0
# wandb>=0.15.0
