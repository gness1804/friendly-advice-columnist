# OpenAI API key (for local development)
OPENAI_API_KEY=sk-your-key-here

# SHA-256 hash of the owner's API key (for fine-tuned model routing)
# Generate with: python -c "import hashlib; print(hashlib.sha256(b'sk-your-key-here').hexdigest())"
OWNER_API_KEY_HASH=

# Model configuration
BASE_MODEL=gpt-4.1-mini
FINE_TUNED_MODEL=ft:gpt-4.1-mini-2025-04-14:personal:friendly-advice-01092026:CwGsaVcA

# AWS configuration (for deployed environment)
AWS_REGION=us-east-2
DYNAMODB_TABLE=friendly-advice-conversations
CONVERSATION_TTL_DAYS=90

# CORS allowed origins (comma-separated, for deployed environment)
ALLOWED_ORIGINS=https://your-domain.com

# LLM parameters
TEMPERATURE=0.3
TOP_P=0.9
MAX_NEW_TOKENS=700
